
[build]
  command = "npm run build"
  publish = "dist"
  functions = "dist/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["express", "stripe", "jsonwebtoken", "firebase-admin"]
  
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
